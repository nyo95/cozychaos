import { mountSpellLab } from './ui/spellLab.js';

/**
 * Stage 0 entry point. PRD §18 — the Spell Lab runs offline with no server,
 * no match loop, and no network, so this is the whole application until the
 * Stage 0 gate is passed.
 */
mountSpellLab();
